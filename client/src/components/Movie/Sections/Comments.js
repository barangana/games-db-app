import React from "react";

function Comments() {
  // We can call the our backend and set the username from the username from localstorage when a user is logged in
  return <h1>here will be the comments section</h1>;
}

export default Comments;
