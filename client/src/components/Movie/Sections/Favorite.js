import React from "react";

function Favorite() {
  // 1- Check if user is logged in or not. (Need to add authentication)
  // 2- Check if the movie is favorited already. If it's already favorited, unfavorite. If not, favorite the movie.
  return (
    <div>
      {"Button will change from Favorite to Unfavorite text with a if function"}
      <h2>this will be the favorite button</h2>
    </div>
  );
}

export default Favorite;
